name: Hello workflow

on: [push]

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Say hello
        run: echo "Hello World!"

      - name: Show date
        run: date

      - name: List directory contents
        run: ls -l
